<?php

/**
 * Service subscription types
 */
class ServiceSubscriptionType
{
    // no subscription = one needs to do it manually as an administrator
    const NONE = "none";
    // open = service provider may subscribe user to this service
    const OPEN = "open";

    // TODO: more types, e.g. invitation type
}
